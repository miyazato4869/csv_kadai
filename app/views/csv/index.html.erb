
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>CSVをインポートする</title>
  </head>
  <body>
    <%= form_with url: csv_upload_path do |f| %>
      <%= f.file_field(:upload_file) %>
      <%= submit_tag "インポートする" %>
    <% end %>
  </body>
</html>